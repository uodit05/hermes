{
  "name": "grabhack-monolith-pg-synapse",
  "private": true,
  "version": "2.0.1",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "install:all": "npm --prefix apps/merchant-ui i && npm --prefix apps/partner-ui i && npm --prefix apps/customer-ui i && npm --prefix apps/ops-ui i",
    "build:ui": "npm --prefix apps/merchant-ui run build && npm --prefix apps/partner-ui run build && npm --prefix apps/customer-ui run build && npm --prefix apps/ops-ui run build",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name init",
    "db:seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "prisma": "^5.22.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
